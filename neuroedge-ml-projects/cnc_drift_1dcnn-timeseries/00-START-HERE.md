# cnc_drift_1dcnn-timeseries — start here

Generated by `/agentforge-ml` on every stage transition; do not edit it, it is rewritten. The work falls into three groups, plus the project's own documents. The folders keep their names because code reads them; this page is the map.

- **Objective:** cnc drift 1D CNN model with 3 sensors
- **Runner (M7):** portal-package
- **Architecture:** 1DCNN

| Group | Milestones | Done | Now |
|---|---|---|---|
| [1. Dataset](#1-dataset) | M0–M6 | 7 of 7 | complete |
| [2. Model](#2-model) | M7–M11, M13 | 6 of 6 | complete |
| [3. Simulator](#3-simulator) | M12 | 1 of 1 | complete |

## 1. Dataset

Lock the use case, then find, price, download, verify, label and extend the data.

- **Folders:** `inputs/, use_case.lock.json, data/`
- **Guide:** [`guides/dataset.md`](guides/dataset.md) (M4) — what the data is, where each number comes from, what is unconfirmed

| | Stage | What it does | Status | Output lands in |
|---|---|---|---|---|
| M0 | `destination` | lock the use case | done | `inputs/, use_case.lock.json, audit/M0.md` |
| M1 | `scout` | find a dataset | done | `data/dataset-card.md` |
| M2 | `plan` | price and scope the download | done | `data/fetch-plan.json` |
| M3 | `download` | download it | done | `data/raw/` |
| M4 | `verify` | measure it, split it, build the contract set | done | `data/splits/, data/contract/, audit/M4.md` |
| M5 | `label` | label it and review the split | done | `data/label-manifest.md, data/split-review.md` |
| M6 | `synth` | synthetic data, or a recorded skip | done | `data/synthetic/, data/synth-review.md` |

## 2. Model

Propose, build, train, evaluate and return the model, then its card.

- **Folders:** `model_proposed.md, model/, 1DCNN/, common/`
- **Guide:** [`guides/model.md`](guides/model.md) (M8) — what the model is, its KPI, how to read the number, what would move it

| | Stage | What it does | Status | Output lands in |
|---|---|---|---|---|
| M7 | `model-select` | propose the model and the runner | done | `model_proposed.md, model/base/` |
| M8 | `model-build` | generate the training code and package | done | `1DCNN/, audit/M8.md` |
| M9 | `train` | train it, on the runner approved at M7 | done | `1DCNN/runs/<run_id>/model-package/` |
| M10 | `eval` | the held-out evaluation | done | `1DCNN/runs/<run_id>/portal_held_out.json` |
| M11 | `return` | build the return package | done | `1DCNN/runs/<run_id>/return/, audit/M11.md` |
| M13 | `model-card` | the model card | done | `model-card.md` |

## 3. Simulator

Replay the model's own split data on the device and in the portal's Virtual Run.

- **Folders:** `sim/, sim-demo/`
- **Guide:** [`guides/demo.md`](guides/demo.md) (M12) — how to run the demo, what to watch, what should happen and when · *not written yet*

| | Stage | What it does | Status | Output lands in |
|---|---|---|---|---|
| M12 | `data-simulator` | simulator data (and a demo replay) | done | `sim/, sim-demo/` |

## 4. Project docs

What this project is, what each group produced, and what crosses to and from the portal.

- **`README.md`** — the objective and the stage log: one row per command run.
- **`guides/`** — `dataset.md`, `model.md`, `demo.md`: written from the files on disk, never over a person's edit.
- **`audit/`** — `/usecase-audit` at M0, M4, M8 and M11: is everything still aligned with the use case?
- **`from-neuroedge/`** — what the portal (or the device) gives this run. Drop downloads there as they are.
- **`to-neuroedge/`** — what this run gives the portal, numbered in upload order. Run `/agentforge-ml handoff` to see what is next, and where each file goes.
- **`run.json`, `gates.json`** — the run's state and every gate decision; the run resumes from these.
