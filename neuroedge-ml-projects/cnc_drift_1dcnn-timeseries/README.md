# cnc_drift_1dcnn-timeseries

- **Objective:** cnc drift 1D CNN model with 3 sensors — detect CNC machining drift from three
  sensor channels and emit a drift score for the edge device.
- **Modality:** timeseries
- **Architecture (requested):** 1D-CNN. A MiniRocket/ROCKET baseline is still required as the
  honesty floor at `/model-select` — a deep net that cannot beat it is not earning its inference
  cost (`time-series-ml`). ONNX is the only deployable format; ROCKET is baseline-only.
- **Destination root:** `NEUROEDGE_ML_ROOT` (`.env:125`)

> **Folder-name note.** The name carries the architecture, which departs from the
> `<intent>-<modality>` convention in `ml-artifact-destination` (ADR-0021). Chosen deliberately at
> M0 on 2026-09-17. Consequence to watch: the MiniRocket baseline lands inside a folder named for
> the 1D-CNN. Keep it here and on the **same splits / same `split_hash`**, or `beats_baseline` at
> M8 is not like-for-like.

Layout per `ml-artifact-destination`: `data/` for cards, manifests and splits; `model-select.md`;
one `<architecture>/` subfolder per built model.

**Start at [`00-START-HERE.md`](00-START-HERE.md)** (generated; AgentForge ADR-0031 D-5): each milestone's
status and where its output landed. Realigned to ADR-0031 on 2026-09-23. `from-neuroedge/` is what the portal
gives this run (formerly `inputs/incoming/`), and `to-neuroedge/` is what it gives the portal, in upload order.
`/agentforge-ml handoff` prints the walk-through. `DATA_README.md` is now
[`guides/dataset.md`](guides/dataset.md). Records written before the move (the M5 review packs,
`data/profile.json`) still cite it by its old name and were left as approved.

## Stage log

| Date | Stage | Command | Artifact | Note |
|---|---|---|---|---|
| 2026-09-17 | M0 `destination` | `/agentforge-ml` | `README.md`, `run.json`, `gates.json` | Fresh start; a prior `cnc_drift-timeseries` scout-only attempt was deleted at the user's request (recoverable at git `db22b17`) |
| 2026-09-17 | M2 `verify` | `/dataset-verify` | `data/profile.json`, `data/splits/{train,val,test}.json`, `data/raw/_metadata_extracts/*` | Confirmed via partial byte-range reads (no full download — archive is 44.6 GB, exceeds the 20 GB stop-and-report threshold): **33 experiments** confirmed (resolves 32-vs-33 TBD), **`x_axis_error` IS derivable** (`DES_POS|n` and `ENC_POS|n` both present as separate columns in `hfdata.csv`), CC BY 4.0 re-confirmed from the archive's own bundled metadata. `vibration_rms` still unresolved (lives in separate `.mat` files, not opened this pass). Full data pull and `portal_upload.zip` deferred pending user go-ahead on the 44.6 GB size. See `data/profile.json` for claimed-vs-measured detail. |
| 2026-09-17 | M2 `verify` (targeted download) | ml-data-engineer, `/agentforge-ml` continuation | `data/raw/Dataset/**` (in progress), `data/profile.json._M2_targeted_download` | User approved a **3.9 GB targeted scope** (all 99 `*.csv`, all 33 `raw_data/*.mat`, ~0.01 GB of small docs — explicitly excluding the 39.27 GB `processed_data/*_synchronized.mat` PCHIP-upsampled files). Re-verified the tar-relative `Data.zip` payload base offset (17920 bytes, via USTAR header walk + GNU base-256 size decode, exact match against the ZIP64 central-directory size) and fetch logic is byte-exact (verified against `DoE.xlsx`, 21360/21360 bytes). Every Range GET now asserts HTTP 206 and exact response length before trusting the bytes, plus a hard 3.9 GB cumulative-budget stop. **RADAR enforces a ~100–250s per-request cooldown independent of client behavior** — completing all 169 approved entries will take several hours. Left running as a **detached background OS process** (not blocking this session); only 2 of 169 files had landed when this pass had to conclude. `vibration_rms` derivation, X-axis NC cross-correlation, at-scale `CTRL_DIFF` check, row counts, label granularity, and `portal_upload.zip` all remain **blocked on bytes not yet on disk**, not on open logic. Resume/inspect via the process log noted in `profile.json`. |
| 2026-09-17 | M2 `verify` | `/dataset-verify` (transfer) | `data/raw/` | **RADAR transfer note.** Three strategies measured, not guessed: 169 per-entry Range requests hit a per-request rate limit (HTTP 429, ~90 s backoff each, 2 files/hour); a single full-body stream avoided 429s but is capped at a measured **0.94 MB/s**, i.e. 13.2 h for all 44.58 GB; **41 merged ranges** (50 MB gap tolerance, 4.43 GB) is the optimum at ~2.3 h. Reuse the merged-range approach for any future KIT/RADAR pull. |
| 2026-09-17 | M2 `plan` | `/agentforge-ml --stage plan` → `/dataset-download` phase 1 | `data/archive-manifest.tsv` (434 rows), `data/fetch-plan.json` | Priced from the saved ZIP64 index, 0 payload bytes. By type: `*_synchronized.mat` 39.27 GB, `raw_data/*.mat` 2.96 GB, `raw_data/*.json` 1.07 GB (78 entries), `hfdata.csv` 0.94 GB, `.fig` 0.34 GB, all else under 0.01 GB. The synchronized `.mat`, raw JSON and `.fig` groups can all be rebuilt from kept files (README.txt l.7, l.11). Scopes: A full 44.58 GB / 13.2 h; **B 169 entries, 3.90 GB payload / 4.89 GB wire / 7.48 GB disk / 35 requests / 2.3 h**; C tool-wear units only, 1.72 GB / 0.9 h. **Scope gate (hard, human): B approved by sanjkcog**, which formalises the informal 3.9 GB approval given earlier the same day; recorded in `gates.json`. Index CRCs were not captured, so the duplicate check could not be evaluated. |
| 2026-09-17 | M3 `download` | `/agentforge-ml` (resume) | `data/raw/**` (gitignored, `.gitignore:14`) | Nothing to fetch: `fetch_plan.pending` = 0 and `verify_on_disk` = 0 missing out of 169 entries in scope. The transfer happened earlier using the merged-range method. |
| 2026-09-17 | M4 `verify` (prep) | `sensor_align` (AgentForge Assets `e9e123d` + `f5cd355`, synced into this repo) | `data/interim/<trial>_sensor.parquet` ×33 (gitignored, 590 MB), `data/interim/alignment.json` | Vibration exists only in `raw_data/*.mat` (MCOS timetables: `pymatreader` returns no data, `mat-io` works). Aligned to the controller clock using the `TAKTGEBER` sync pulse: the anchor is the `G04 F2` dwell, and the rate comes from fitting the sync edges. **33/33 aligned**: DAQ clock 75–81 ppm fast (up to 98 ms over a run, now removed); anchor good to about one sync phase (vibration leaves baseline a median 8 ms before spindle current, range −14..+8 ms). `vibration_rms` joins `hfdata.csv` on `CYCLE`. Still open: acceleration unit, and whether the edge device will carry an accelerometer (channel contract). DATA_README §5 rewritten. |
| 2026-09-17 | M4 `verify` (taxonomy + re-split) | `/agentforge-ml` (user decision) | `data/splits/{train,val,test}.json`, `data/label-manifest.md`, `data/profile.json._taxonomy_decision_M4`, `dataset-card.md` `split_hash` | The old `anomaly` label mixed three phenomena. Now `normal` vs `tool_wear` (IM-01R-A01…A05). Excluded: 6 commanded-setting trials (leakage) and 4 part-defect trials (different use case). Hybrid split: by experiment (train 16 / val 5 / test 4 entries); worn trials A01/A02/A05 train, A04 val, A03 test. The IM-01R normal run is split chronologically 60/20/20 with 5 s gaps, so val and test each hold a same-program normal. That is a **documented deviation** from the rule that no unit spans train and test; M7 window must be ≤ 5 s, and `ml-eval-reviewer` checks it at M8. New `split_hash bca8a336…` replaces `6e204992…`. |
| 2026-09-17 | M4 `verify` (gate) | `/agentforge-ml` | `data/profile.json` (`_M4_measured_3ch`, `_channel_contract_M4`), `data/portal_upload.zip` (53 MB, train+val only, gitignored), `gates.json` | Measured on the 3 chosen channels: 0 missing values, 0 CYCLE gaps, ≥ 88.7% of CSV rows matched. Hours: train 1.27 normal / 1.00 tool_wear; val 0.35 / 0.34; test 0.22 / 0.33. The portal trainer (`ts_preprocessor.py`) windows across files and splits windows at random, so use the package runner. **Gate: approved by sanjkcog.** 3 channels; field accelerometer and unit `g` are **assumed, unconfirmed**. **Open:** the portal use case declares `[spindle_load %, vibration g, coolant_temp, feed_rate]` at 100 Hz with a 512-sample window. That must be reconciled with the trained inputs before M7/M8. |
| 2026-09-18 | M4 `verify` (review fixes) | `code-reviewer` + `ml-eval-reviewer` → `sensor_align` fix (Assets `83e0340`, synced) | `data/interim/IM-01R_sensor.parquet`, `data/interim/alignment.json`, `data/portal_upload.zip` (rebuilt), `data/profile.json`, `data/label-manifest.md` | **HIGH, fixed:** IM-01R's channel means were taken over the whole recording, including its val/test segments. It was re-aligned with means from its train segment only; rows and drift are unchanged, and the zip and stats were rebuilt. Also fixed in the tool: kept ticks no longer depend on each channel's NaNs, and the batch survives an unexpected error. M7/M8 requirements from the review (window guard, windows from split bounds, per-unit metrics, n = 5 caveats) are recorded in `label-manifest.md`. |
| 2026-09-18 | M0 re-lock + M3 re-split + M4 contract dataset | `/agentforge-ml` (NeuroEdge-Web ADR-0008; Assets `aeaa638`, `78f37db`) | `use_case.lock.json` (`8c2a044d`), `data/contract_sources.json`, `data/contract/` (23 units, gitignored parquet + tracked `manifest.json`), `data/splits/*.json` (`split_hash 47871db0`), `data/portal_upload.zip` (rebuilt) | The use case was corrected in the portal (W1: 3 channels, 10 Hz, window 64, stride 10, classes normal/tool_wear) and **locked**. The lock reads `at_fpr 0.01` from the KPI link. IM-01R was **re-split with 10 s gaps**, because the 6.4 s contract window (3200 ticks) exceeded the old 5 s gap, and re-centred on its new train segment. **Contract dataset** built: 10 Hz, 100 ms mean torque (Nm) / mean following error ×1000 (µm) / exact pooled vibration RMS (g), blocks inside split bounds; a raw spot-check matches exactly. Every downstream consumer now reads `data/contract/`. Open: the use case's `min_value 0.5` contradicts its own "recall ≥ 0.90" note (use-case content). |
| 2026-09-18 | simulator export (val only, ahead of M12) | `ml_contract.export_sim --splits val` | `sim/manifest.json` (tracked), `sim/val/*.csv` (gitignored) | Five val CSVs at 10 Hz in contract order, stamped with lock `8c2a044d`, each marked `debug_and_parity`. Two of them (IM-01R-A04 tool wear, the IM-01R normal val segment) are the real-data device fixture `NeuroEdge-Device/src/ne-data-plane/tests/fixtures/cnc_drift_10hz` (Device `b43322a`), for device-vs-`eval.py` parity. The formal M12 `data-simulator` stage still runs after M11. |
| 2026-09-18 | M0 re-lock (final review) | `/agentforge-ml` (Assets `8c2f20a`; Web `0691e62`) | `use_case.lock.json` (`3edb2d20`, replaces `8c2a044d`), `data/contract/manifest.json`, `sim/manifest.json`, `data/portal_upload.zip` (rebuilt) | The final code review found `reduce` was never produced. The use case now declares it per channel (mean / mean / rms), and the lock requires it. The source hash is now line-ending-stable (CRLF == LF). Re-locked; the contract dataset was rebuilt (it now also refuses sources whose aggregation differs from the lock's reduce) and the val simulator export re-stamped. The Device fixture was updated to the new lock. |
| 2026-09-18 | M0 re-lock (recall gate) | `/agentforge-ml` | `use_case.lock.json` (`b7a3765f`, replaces `3edb2d20`), `data/contract/manifest.json`, `sim/manifest.json`, `data/portal_upload.zip` (README lock line only), `run.json` | User confirmed the KPI: **recall ≥ 0.90 at FPR 0.01**. The use case stated 0.90 only in its notes and carried the wizard default `min_value 0.5` in the gate; `min_value` is now 0.9 (Step 1 now warns when notes and gate disagree). Re-locked; contract dataset and val simulator export rebuilt (data identical, new lock stamp). The M10 KPI gate compares against 0.9. Device fixture re-stamped. |
| 2026-09-18 | M6 `synth` | `/synth-data --signal` | `data/synth/gen_synth.py`, `data/synthetic/manifest.json` (+ 60 gitignored parquet), `data/synthetic-recipe.md`, `.gitignore` (new synthetic-blob rule) | Real-anchored block-bootstrap of the train-only IM-01R normal head, plus a wear delta fitted to A01/A02/A05 (x_axis_error std +28%, vibration_rms std -18%, spindle_load std +3–10%), generated 30 synthetic `tool_wear` + 30 matched synthetic `normal` IM-01R-program runs (seed `20260918`, byte-identical reruns confirmed); real:synthetic tool_wear window ratio ≈ 1:10, with M8 recommended to cap synthetic at ≤ 50% of real tool_wear train windows and compare real-only vs. real+synth on the real val split only. |
| 2026-09-18 | M7 `model-select` | `/model-select` | `model-select.md` | Picked a small dilated 1D-CNN (~11.4k params, plain conv stack over InceptionTime), trained from scratch (no pretrained TS backbone fits the edge budget), two-class supervised BCE over one-class/normal-only, capped real+synth mix with a required real-only-vs-real+synth ablation on real val, MiniRocket (`aeon`, BSD-3) as the honesty-floor baseline, `package` runner, ONNX opset 13 export with in-graph global (not per-window) normalization and a folded Sigmoid. |
| 2026-09-19 | M8 `model-build` | `/model-build --pytorch` (ml-modeler) | `1DCNN/{model.py,ts_data.py,metrics.py,packaging_.py,train.py,eval.py,config.yaml,requirements.txt,RUN_ON_GPU.md,data/README.md}`, `MiniRocket/{ts_data.py,metrics.py,train.py,eval.py,config.yaml,requirements.txt,RUN_ON_GPU.md}` | Built the approved `model_proposed.md` architecture verbatim (11,393 parameters, matches the computed count) and a full training-ready handoff package: real-vs-real+synthetic ablation (judged on real val only), leave-one-worn-unit-out CV over train+val worn units (A01/A02/A05/A04, never A03/test), per-unit max/mean rare-event metrics with the required blowhole-vs-pure-wear and same-program-vs-cross-program segmentations, MiniRocket+ridge baseline via `aeon` feeding `beats_baseline` into the CNN's `model_artifact.json`, ONNX opset 13 export with in-graph global normalization + folded terminal Sigmoid. `train.py`/`eval.py` never open `test.json` except `eval.py` via an explicit `--split` argument. Smoke-tested (no real data, no GPU, no training run): `model.py` forward pass + parameter count, `train.py`'s full train/val loop + ONNX export + package write on synthetic tensors, the leave-one-worn-unit-out CV loop, the scaffold `NEUROEDGE_CONTEXT` extraction, and `eval.py`'s per-unit/segment reporting -- all against fabricated windows, since this session's Python environment has no `pyarrow` to open the real `data/contract/*.parquet` (flagged to the user; verify column names against the real files on the GPU box before a full run). One correctness bug found and fixed during smoke-testing: `load_lock`'s hash check was hashing the raw file bytes; `lock_sha256` is actually sha256 of the canonical JSON (sorted keys, compact separators) with the `lock_sha256` field itself excluded -- fixed and re-verified against the real `use_case.lock.json`. |
| 2026-09-19 | M8 `model-build` fix (ml-eval-reviewer BLOCK, HIGH wrong-metric) | `/model-build --pytorch` (ml-modeler) | `1DCNN/{ts_data.py,metrics.py,train.py,eval.py,config.yaml,RUN_ON_GPU.md}`, `MiniRocket/{train.py,eval.py,ts_data.py}` | Fixed the gating `recall_at_fpr`/`fpr` to be the PER-UNIT (max-aggregation) number everywhere it gates a decision (epoch selection primary criterion, ablation winner, `beats_baseline`, `metrics.json`/`model_artifact.json` top level, `eval.py`'s single-trial caveat) per `data/label-manifest.md` requirement #4 -- window-level numbers now ride along clearly suffixed `_window` as diagnostics only (`metrics.label_as_window`). Normalisation is now fit on real-train windows only in BOTH the `real_only` and `real_synthetic` ablation variants (previously the `real_synthetic` variant fit it on the mixed set, an overfit risk given synthetic std runs 81-91% of real on two channels). `beats_baseline` now carries `baseline_trained_on`/`winner_trained_on` alongside it. `config.yaml`'s `training.monitor`/`tie_break` and `task.target_recall` are now wired into the selection logic and `meets_target` reporting instead of sitting unused. The synthetic cap now floors instead of rounds (`data/synth-review.md`'s 2,245/1,794, not round()'s 2,246/1,794). Re-verified real train/val window counts via `uv run --offline --no-project --with pyarrow --with pandas --with numpy` from `1DCNN/`: train 4,491/3,588, val 1,244/1,204 (exact match), and the floored synthetic cap: 2,245/1,794 (exact match to `data/synth-review.md`). Re-ran all smoke checks (train/val loop on fake tensors including a package write, LOWO-CV, `eval.py`) after the fix -- all pass. |
| 2026-09-19 | M8 `model-build` fix round 3 (ml-eval-reviewer BLOCK, CRITICAL wrong-metric) | `/model-build --pytorch` (ml-modeler) | new `common/{__init__.py,ts_data.py,metrics.py}`; `1DCNN/{train.py,eval.py,config.yaml,RUN_ON_GPU.md,data/README.md}`; `MiniRocket/{train.py,eval.py,config.yaml,RUN_ON_GPU.md}`; deleted the per-package `ts_data.py`/`metrics.py` duplicates | **Replaced round 2's per-unit max-aggregation "headline" with the human-chosen Option 3** after ml-eval-reviewer showed it saturates almost regardless of the model: flagging a unit whenever the MAX of its 183-602 windows crosses a threshold calibrated at a 1% POOLED window FPR gives ~3.74-of-4 normal units alarming on pure noise (headline FPR ~0.75-1.0, recall ~1.0, invalidating selection/ablation/`beats_baseline`/`meets_target`/LOWO). The new headline everywhere it gates a decision (epoch selection, ablation winner, `beats_baseline`, `metrics.json`/`model_artifact.json` top level) is the **POOLED WINDOW-level** `recall_at_fpr`/`fpr`/`pr_auc`/`confusion_matrix` (`aggregation: "window_pooled"`, no more `_window`-suffixed ambiguity), calibrated at the lock's `at_fpr` on real val windows. `meets_target` now requires BOTH `recall_at_fpr >= target_recall` AND `fpr <= at_fpr + fpr_tolerance` (a new, named `evaluation.fpr_tolerance: 0.005`), reported as `meets_recall`/`meets_fpr` too. Per-unit reporting (`data/label-manifest.md` #4) is now a descriptive **rate table** (window recall for worn units; false-alarm rate + false-alarms-per-hour, derived from the lock's stride/rate, for normal units) plus separate, explicitly diagnostic-only **event metrics** (`evaluation.alarm_persistence_windows: 1`, event recall, time-to-first-alarm) -- never a second gating construction. The required blowhole/pure-wear and same-program/cross-program segmentations, and the single-trial caveat (now attached directly to the `IM-01R-A03` rate-table row), are preserved. LOWO-CV per fold now reports the held-out unit's window recall and the fold's pooled FPR (threshold calibrated on that fold's held-in normal windows only). **Code-review HIGHs fixed:** (A) `ts_data._unit_split_rows` now RAISES when a unit/split is missing from `data/contract/manifest.json`, instead of silently skipping the row-count check; (B) `MiniRocket/eval.py`'s `joblib.load` now refuses any `--model` path resolving outside the `MiniRocket/` folder (absolute-elsewhere or `../`-escape), with a pickle-safety warning added to its `RUN_ON_GPU.md`; (C) `ts_data.py`/`metrics.py` were hand-duplicated between `1DCNN/` and `MiniRocket/` -- moved into one shared `common/` package imported via a `sys.path` insert relative to `Path(__file__).resolve().parents[1]`, and the per-package copies deleted. Re-verified real data loading through the new `common` module via `uv run --offline --no-project --with pyarrow --with pandas --with numpy` from `1DCNN/`: train 4,491/3,588, val 1,244/1,204, synthetic cap 2,245/1,794 -- all exact matches, unchanged by the refactor. Added an explicit smoke test reproducing ml-eval-reviewer's exact saturation scenario (4 normal units sized 602/225/234/183 + 1 worn unit, pure random-noise scores): the new pooled headline gives `fpr ≈ 0.0096` (≈ `at_fpr` 0.01, not ~0.75-1.0) and `meets_target: false`, while the old (now-deleted) per-unit max-boolean construction reproduced on the same noise gives `fpr = 1.000`, confirming the fix. Re-ran every other smoke check (train/val loop both variants, package write, LOWO-CV, `eval.py` against a fabricated package, the MiniRocket pickle-safety guard and its metrics functions since `aeon` is unavailable in this sandbox) -- all pass. |
| 2026-09-19 | M8 `model-build` close | `/agentforge-ml` (main session) | `common/metrics.py`, `1DCNN/{train.py,eval.py,config.yaml}`, `MiniRocket/{train.py,eval.py,config.yaml}` | ml-eval-reviewer round 3 PASS, code-reviewer APPROVE. Human decision at the eval-methodology gate: `fpr_tolerance` set to 0 (the KPI is exactly FPR <= 0.01), with `fpr_tolerance` and `effective_fpr_bound` written beside `meets_fpr`; the val headline now carries `headline_positive_units` and a single-trial caveat (val has one worn unit, A04); `eval.py` carries the package's LOWO `cross_validation` spread into the test report. Smoke checks re-run, all pass. Gate `eval-methodology` approved; M8 complete. |
| 2026-09-19 | M9 `train` | external (runner `package`) | `1DCNN/HANDOFF.md` | Waiting on the external training run (`run.json` gate `waiting_external`). Expected package: `1DCNN/runs/<run_id>/model-package/`. Resume with `/agentforge-ml --resume`. |
| 2026-09-19 | M7 `model-select` correction (doc-only) | `/agentforge-ml` (main session) | `model_proposed.md` §Runner, `data/label-manifest.md` requirement #2, `1DCNN/HANDOFF.md` | The recorded reason for `runner: package` was stale: the portal's `ts_preprocessor.py` was fixed on 2026-09-18 (NeuroEdge-Web `1197d27`) and no longer windows before splitting. The decision stands for other reasons, read from the portal code: a random unit shuffle that cannot take the approved split, test not kept withheld, a TS trainer with no calibrated threshold or recall@FPR, and no baseline. No gate re-opened: the approved decision is unchanged. |
| 2026-09-23 | Realign to AgentForge ADR-0031 (D-9), still at M9 | `handoff stage`/`index`, `intake check`, `data_simulator --profile demo`, AgentForge Assets `774d72e` installed | `from-neuroedge/` (was `inputs/incoming/`), `to-neuroedge/` (01, 02, 05, 06 staged), `00-START-HERE.md`, `guides/dataset.md` (was `DATA_README.md`, Part 2 rewritten from files on disk: trial taxonomy, 500 Hz to 10 Hz, real windows, splits with test counts, synthetic, unconfirmed list), `guides/model.md` (new), `sim-demo/` | No load-bearing path moved; nothing re-gated. `1DCNN/portal_held_out.json` (2026-09-21) stays where it is until its model package is dropped in `from-neuroedge/` and `intake` places both under `1DCNN/runs/<run_id>/`. The staged 01 and 02 match the hashes the portal's held-out result names, so they already reached the portal; they are not recorded as sent. |
| 2026-09-23 | M13 `model-card` | `/agentforge-ml` (ml-modeler) | `model-card.md` | Written from files on disk only: `data/dataset-card.md` (dataset id/licence/attribution), `1DCNN/runs/bd19c981a17d/model-package/{model_artifact.json,meta.json,metrics.json}`, `use_case.lock.json`, `data/splits/*.json`, `1DCNN/portal_held_out.json`, `1DCNN/baseline_check.json`, `gates.json` (`kpi`, `return-upload`), `inputs/inputs.json`. Status: **BELOW TARGET — demo/internal use only** (held-out recall@FPR0.01 = 0.318 vs target 0.90; FPR 0.0090 met; LOWO spread 0.24-0.37). Notes both `split_hash` values (`47871db0…` on the split files, `e89654ee…` = `common/ts_data.split_hash_of('train','val')`) refer to the same split. No run.json/gates.json edits; no training or eval.py run; portal not called. |
