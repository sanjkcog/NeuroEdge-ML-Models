# One environment with every other step of the package: exact pins only (ADR-0028 D-5).
# Checked with `package check-env` for Linux x86_64, Python 3.11, wheels only. Never executed: see RUN_ON_GPU.md.
torch==2.5.1
transformers==4.46.3
optimum==1.23.3
onnx==1.17.0
onnxruntime==1.20.1
numpy==2.1.3
pillow==11.0.0
pyyaml==6.0.2
