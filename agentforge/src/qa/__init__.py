"""AgentForge QA traceability — pluggable test-management adapters (EP-04).

Everything here works with no vendor configured (D12): `NoneAdapter` is a first-class
option, not a stub. See ADR-0003 for the TC-ID dialect mapping and the Zephyr-first
vendor decision.
"""
