---
description: Quick commit with natural language file targeting — describe what to commit in plain English
argument-hint: [target description] (blank = all changes)
---

## Arguments

$ARGUMENTS — a plain-English description of what to commit (blank = stage and commit all changes).

## Procedure

This command's full procedure lives in the `prp-commit` skill so a subagent can run it identically. **Read and execute `agentic-assets/skills/SDLC/prp/prp-commit.md`**, treating `$ARGUMENTS` as its input.

## NeuroEdge Assets

> At the start of your response output exactly:
> `[ NeuroEdge Assets ]  /prp-commit · Skills: prp-commit, git-workflow`
>
> Then read these skill files before executing:
> - `agentic-assets/skills/SDLC/prp/prp-commit.md`
> - `agentic-assets/skills/SDLC/development/git-workflow.md`
<!-- neuroedge-assets-patched source-version=426fa40 -->
